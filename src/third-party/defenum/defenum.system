;;;; -*- Mode: Lisp -*-

;;;; defenum.system --
;;;;
;;;; See the file COPYING for copyright and licensing information.

(mk:defsystem "DEFENUM"
  :author "Marco Antoniotti"
  :licence "BSD"
  :documentation "The DEFENUM facility provides C++ and Java styled 'enum' in Common Lisp."
  :components ((:file "defenum-package")
               (:file "lambda-list-parsing"
                :depends-on ("defenum-package"))
               (:file "defenum"
                :depends-on ("defenum-package"
                             "lambda-list-parsing"))

               #+cl-enumerations
               (:file "enum-enumeration"
                :depends-on ("defenum"))
               )
  )

;;;; end of file -- defenum.system --
